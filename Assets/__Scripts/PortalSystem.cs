using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PortalSystem : MonoBehaviour
{
    public Transform positionToTransport;
    // Start is called before the first frame update
}
